# 🗓️ Day 14: Matrix Operations & Coordinate Mapping

**Topic:** Numerical matrices and 2D searching algorithms.

---

## 🛠️ Key Concepts

### 1. Matrix Numerical Logic
Explored different mathematical ways to populate an $N \times N$ matrix:
* **Sequential Fill:** Incrementing a counter for a row-major fill.
* **Product Matrix:** Using `i * j` to create a multiplication table.
* **Column-Major Fill:** Using `i + (j * rg)` to populate data vertically.

### 2. Searching in 2D Space
Implemented a search algorithm to locate a target value within a generated matrix:
* **Coordinate Return:** The program returns the exact $(x, y)$ pair if the value is found.
* **Flag Logic:** Uses a boolean "found" flag to handle missing targets.

---

## 📝 Files in this Folder
* **day14_pbssd_pattern_printing.py**: Advanced numerical and coordinate-based patterns.